# Benchmark Report

> Generated by `cmd/bench` from the workloads in `docs/SPEC.md`. Results are hardware-specific. Measured pipelines use lossless delivery and the discard logger sink.

## Environment and configuration

| Field | Value |
|---|---|
| Run time | 2026-07-18T08:37:31Z |
| CPU | Apple M2 (8 logical CPUs) |
| OS / architecture | darwin / arm64 |
| Go | go1.26.5 |
| GOMAXPROCS | 8 |
| GOGC / GOMEMLIMIT | 100 (default) / off (default) |
| Fixture | `btc_orderbook_updates.csv`; 2242 rows; SHA-256 `f76d799b611f7de82eadd2758f611dd05fb5b547c941ad14cdabf6eb0fa1603f` |
| Fixture sync | timestamp-step=100 source units; unit=ms |
| Synthetic | seed=42; positions=10000000; max levels/side=64; mix=35% existing, 20% delete, 25% new, 20% best-touch |
| Rings | event=65536; log=65536; spin=128 |
| Record sizes | pipeline.Event=104 B; logx.Record=112 B |
| Logger | discard; lossless; zero drops required |
| Fixture repetitions | 20 measured after 3 warmups |

## Throughput

The fixture is a correctness and representative-latency workload. W6a is the synthetic headline. W6 emits one record at a time and never retains the high-volume stream.

| Workload | Events | Duration | Updates/s | Runtime allocs/op¹ | Runtime B/op¹ |
|---|---:|---:|---:|---:|---:|
| W2 apply-only fixture mix | 44840 | 4.286ms | 10.46M | 0.0144 | 95.40 |
| W3 decode+apply | 44840 | 19.795ms | 2.27M | 2.1224 | 174.87 |
| W1 fixture timestamp-step pipeline | 44840 | 32.211ms | 1.39M | 2.1322 | 6519.08 |
| W4/W6a delta-only full pipeline | 10000001 | 2.765419s | 3.62M | 0.0000 | 0.00 |
| W6b periodic-snapshot full pipeline | 10000001 | 2.723017s | 3.67M | 0.0002 | 0.09 |

¹ Process-wide `runtime.MemStats` deltas are supporting end-to-end indicators. `go test -bench . -benchmem` is authoritative for microbenchmark allocation counts.

## W8 synchronization-policy cost

Identical seeded streams carry timestamp steps and contiguous update-ID ranges.

| Policy | Events | Updates/s |
|---|---:|---:|
| timestamp-step | 1000001 | 3.71M |
| synchronization-off | 1000001 | 3.44M |
| update-ID | 1000001 | 3.55M |

## Callback latency

`IngressNS -> receiveNS` is primary. All endpoints share one local monotonic clock; historical CSV epochs are never subtracted from process time.

```text
W1 ingress->callback: count=44840 p50=250ns p90=8.89µs p95=18.8µs p99=64.8µs p99.9=134µs max=165µs
W1 apply->callback: count=44840 p50=125ns p90=8.77µs p95=18.6µs p99=64.3µs p99.9=134µs max=164µs
W6a ingress->callback: count=10000001 p50=250ns p90=4.05µs p95=57.1µs p99=358µs p99.9=1.29ms max=1.88ms
W6a apply->callback: count=10000001 p50=166ns p90=3.82µs p95=57.1µs p99=358µs p99.9=1.29ms max=1.88ms
W6a apply duration: count=10000001 p50=83ns p90=84ns p95=125ns p99=208ns p99.9=333ns max=429µs
```

## W7 paced replay diagnostics

One fixture pass at 10000x, with source offsets rebased onto the local monotonic timeline.

```text
due->callback: count=2242 p50=8.77µs p90=23.9µs p95=29.1µs p99=41.2µs p99.9=70.1µs max=89.8µs
scheduler lateness: count=2242 p50=6.69µs p90=18µs p95=23.7µs p99=36.6µs p99.9=66µs max=75.9µs
ingress->callback: count=2242 p50=835ns p90=8.26µs p95=12µs p99=21.6µs p99.9=30.3µs max=44.3µs
```

## Queueing and backpressure

- W5 delivered 20000/20000 FIFO events with zero loss; delayed consumption caused 59 producer waits; peak depth 256/256.
- W1 sampled event peak 167/65536; discard-log peak 322/65536; logger waits 0; drops 0.
- W6a sampled event peak 6756/65536; log peak 37044/65536; logger waits 0; drops 0.
- W6b sampled event peak 3809/65536; log peak 31648/65536; logger waits 0; drops 0.

## Reproduce

```sh
go test -bench . -benchmem -run '^$' ./...
go run ./cmd/bench -csv btc_orderbook_updates.csv -synthetic 10000000 -out BENCHMARK.md
```

Run multiple times after warmup when comparing changes. Paced latency is a scheduler diagnostic, not a fast-throughput metric.
